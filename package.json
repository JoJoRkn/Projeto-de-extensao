{
  "name": "workswipe",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "npm install",
    "dev": "node scripts/dev.js",
    "start": "npx http-server -c-1 -p 8000",
    "start:py": "python -m http.server 8000",
    "payment-server": "node payment-server.js",
    "dev:full": "concurrently \"npm run payment-server\" \"npm start\"",
    "seed:admin": "node scripts/seed_firestore_admin.js",
    "setup:seed": "npm run seed:admin"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "firebase": "^12.4.0",
    "mercadopago": "^2.9.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "firebase-admin": "^11.11.0",
    "http-server": "^14.1.1"
  }
}
